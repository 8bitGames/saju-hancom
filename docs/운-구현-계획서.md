# 사주 운(運) 시스템 구현 계획서

> **작성일**: 2026-01-02
> **참고 영상**: [강헌 명리학자 - 사주 똑같아도 인생 격차가 확 벌어지는 이유](https://www.youtube.com/watch?v=4LZd2T-YxjI)

---

## 1. 현황 분석

### 1.1 현재 구현 상태

| 운(運) | 상태 | 위치 | 설명 |
|--------|------|------|------|
| **대운 (大運)** | ✅ 구현됨 | `pipeline-types.ts` Step5 | 10년 단위, AI 추정 방식 |
| **세운 (歲運)** | ✅ 구현됨 | `temporal-agent.ts` | 연도별 천간지지 |
| **월운 (月運)** | ⚠️ 부분 구현 | `temporal-agent.ts` | 3-4개월만 분석 |
| **일운 (日運)** | ❌ 미구현 | - | 하루 단위 운세 |
| **시운 (時運)** | ❌ 미구현 | - | 2시간 단위 운세 |
| **소운 (小運)** | ❌ 미구현 | - | 어린시절 운 |

### 1.2 현재 아키텍처

```
lib/saju/
├── calculator.ts          # 사주 계산 (lunar-javascript 사용)
├── pipeline-orchestrator.ts   # 6단계 분석 오케스트레이션
├── pipeline-steps.ts      # 각 단계 AI 분석
├── pipeline-types.ts      # 타입 정의
├── pipeline-schemas.ts    # Zod 스키마
├── agents/
│   └── temporal-agent.ts  # 세운/월운 계산
├── elements.ts            # 오행 분석
├── ten-gods.ts            # 십성 분석
└── stars.ts               # 신살 분석
```

---

## 2. 설계 원칙

### 2.1 강헌 명리학자 핵심 철학 반영

> "사주팔자는 **자동차**고, 대운은 그 차가 달리는 **도로**다"

| 개념 | 비유 | UI 반영 |
|------|------|---------|
| 사주 원국 | 자동차 (타고난 것) | 기본 프로필 섹션 |
| 대운 | 고속도로 (10년 환경) | "현재 달리는 도로" |
| 세운 | 올해 도로 상태 | "올해의 여정" |
| 월운 | 월별 도로 상황 | "이번 달 컨디션" |
| 일운 | 오늘 도로 상황 | "오늘의 운전 조건" |
| 시운 | 시간대별 교통량 | "지금 이 순간" |

### 2.2 계산 방식 분류

| 영역 | 방식 | 설명 |
|------|------|------|
| 천간지지 도출 | **규칙 기반** | 육십갑자 순환, 100% 정확 |
| 합충형파해 분석 | **규칙 기반** | 결정적 관계 판별 |
| 의미 해석 | **AI 기반** | Gemini로 맥락적 해석 |
| 실용적 조언 | **AI 기반** | 개인화된 조언 생성 |

---

## 3. 구현 계획

### Phase 1: 일운 & 월운 상세화 (높은 우선순위)

#### 3.1.1 새 모듈 구조

```
lib/saju/fortune/
├── types.ts               # 운 관련 타입 정의
├── calculator.ts          # 공통 계산 유틸리티
├── daily-fortune.ts       # 일운 계산
├── monthly-fortune.ts     # 월운 계산 (12개월)
├── hourly-fortune.ts      # 시운 계산 (Phase 2)
├── major-fortune.ts       # 대운 정밀화 (Phase 2)
├── minor-fortune.ts       # 소운 계산 (Phase 3)
└── index.ts               # 모듈 export
```

#### 3.1.2 일운 (日運) 구현

**타입 정의:**
```typescript
interface DailyFortune {
  date: string;              // YYYY-MM-DD
  stem: string;              // 일간 (甲, 乙, ...)
  branch: string;            // 일지 (子, 丑, ...)
  stemKorean: string;        // 일간 한글
  branchKorean: string;      // 일지 한글
  element: string;           // 오행

  // 원국과의 관계
  natalInteraction: {
    harmonies: string[];     // 합 관계
    clashes: string[];       // 충 관계
    punishments: string[];   // 형 관계
  };

  // 용신과의 관계
  usefulGodRelation: 'support' | 'neutral' | 'against';

  // 분석 결과
  score: number;             // 0-100
  grade: 'excellent' | 'good' | 'normal' | 'caution';
  theme: string;             // 오늘의 테마
  advice: string;            // 조언

  // 활용 가이드 (강헌 철학 반영)
  opportunities: string[];   // 좋은 일
  cautions: string[];        // 주의할 일
  activities: string[];      // 추천 활동
}
```

**일진 계산 로직:**
```typescript
// 기준일: 1900년 1월 1일 = 甲子일
const BASE_DATE = new Date(1900, 0, 1);
const BASE_INDEX = 0; // 甲子 = 0

function calculateDailyPillar(date: Date): { stem: string; branch: string } {
  const diffDays = Math.floor((date.getTime() - BASE_DATE.getTime()) / (1000 * 60 * 60 * 24));
  const index = ((diffDays % 60) + 60) % 60; // 음수 처리

  const stemIndex = index % 10;
  const branchIndex = index % 12;

  return {
    stem: HEAVENLY_STEMS[stemIndex],
    branch: EARTHLY_BRANCHES[branchIndex]
  };
}
```

#### 3.1.3 월운 12개월 상세화

**pipeline-types.ts 수정:**
```typescript
interface Step5_FortuneTiming {
  // 기존 필드들...
  currentMajorFortune: MajorFortune;
  nextMajorFortune: { ... };
  yearlyFortune: YearlyFortune;

  // 새로 추가
  monthlyFortunes: MonthlyFortune[];  // 12개월 전체

  // 기존 (유지하되 deprecated)
  monthlyHighlights: MonthlyHighlight[];

  timingAdvice: { ... };
}

interface MonthlyFortune {
  month: number;           // 1-12
  stem: string;            // 월간
  branch: string;          // 월지
  stemKorean: string;
  branchKorean: string;
  element: string;

  // 세부 분석
  natalInteraction: string;  // 원국과 관계
  usefulGodRelation: string; // 용신과 관계

  score: number;           // 0-100
  grade: 'excellent' | 'good' | 'normal' | 'caution';
  theme: string;           // 이달의 테마

  // 실용 정보
  opportunities: string[];
  challenges: string[];
  advice: string;

  // 추천
  luckyDays: number[];     // 좋은 날
  cautionDays: number[];   // 주의할 날
}
```

---

### Phase 2: 시운 & 대운 정밀화 (중간 우선순위)

#### 3.2.1 시운 (時運) 구현

**시지(時支) 매핑:**
```typescript
const HOUR_TO_BRANCH: Record<number, string> = {
  23: '子', 0: '子',   // 자시: 23:00-01:00
  1: '丑', 2: '丑',    // 축시: 01:00-03:00
  3: '寅', 4: '寅',    // 인시: 03:00-05:00
  5: '卯', 6: '卯',    // 묘시: 05:00-07:00
  7: '辰', 8: '辰',    // 진시: 07:00-09:00
  9: '巳', 10: '巳',   // 사시: 09:00-11:00
  11: '午', 12: '午',  // 오시: 11:00-13:00
  13: '未', 14: '未',  // 미시: 13:00-15:00
  15: '申', 16: '申',  // 신시: 15:00-17:00
  17: '酉', 18: '酉',  // 유시: 17:00-19:00
  19: '戌', 20: '戌',  // 술시: 19:00-21:00
  21: '亥', 22: '亥',  // 해시: 21:00-23:00
};
```

**오호둔시법 (시간 결정):**
```typescript
// 일간에 따른 자시(子時) 시작 천간
const DAY_STEM_TO_HOUR_START: Record<string, string> = {
  '甲': '甲', '己': '甲',  // 갑기일 → 갑자시 시작
  '乙': '丙', '庚': '丙',  // 을경일 → 병자시 시작
  '丙': '戊', '辛': '戊',  // 병신일 → 무자시 시작
  '丁': '庚', '壬': '庚',  // 정임일 → 경자시 시작
  '戊': '壬', '癸': '壬',  // 무계일 → 임자시 시작
};

function calculateHourlyPillar(dailyStem: string, hour: number): { stem: string; branch: string } {
  const branch = HOUR_TO_BRANCH[hour];
  const branchIndex = EARTHLY_BRANCHES.indexOf(branch);

  const startStem = DAY_STEM_TO_HOUR_START[dailyStem];
  const startStemIndex = HEAVENLY_STEMS.indexOf(startStem);

  const stemIndex = (startStemIndex + branchIndex) % 10;

  return {
    stem: HEAVENLY_STEMS[stemIndex],
    branch: branch
  };
}
```

#### 3.2.2 대운 정밀화

**절기 기반 대운수 계산:**
```typescript
interface MajorFortuneCalculation {
  startAge: number;           // 대운 시작 나이
  direction: 'forward' | 'backward';  // 순행/역행
  fortunes: MajorFortunePillar[];     // 대운 목록
}

function calculateMajorFortuneStartAge(
  birthDate: Date,
  gender: 'male' | 'female',
  yearStem: string
): number {
  // 순행/역행 판정
  const isYangStem = ['甲', '丙', '戊', '庚', '壬'].includes(yearStem);
  const isForward = (gender === 'male' && isYangStem) ||
                    (gender === 'female' && !isYangStem);

  // 절기까지 일수 계산
  const daysToSolarTerm = isForward
    ? getDaysToNextSolarTerm(birthDate)  // 다음 절기
    : getDaysToPreviousSolarTerm(birthDate);  // 이전 절기

  // 3일 = 1년, 나머지 처리
  const years = Math.floor(daysToSolarTerm / 3);
  const remainder = daysToSolarTerm % 3;

  // 나머지 2 이상이면 +1
  return remainder >= 2 ? years + 1 : years;
}
```

---

### Phase 3: 소운 & UX 개선 (낮은 우선순위)

#### 3.3.1 소운 (小運) 구현

```typescript
interface MinorFortune {
  age: number;         // 1세 ~ 대운시작 전
  stem: string;
  branch: string;
  element: string;
  description: string;
}

function calculateMinorFortunes(
  hourPillar: { stem: string; branch: string },
  gender: 'male' | 'female',
  yearStem: string,
  majorFortuneStartAge: number
): MinorFortune[] {
  // 소운도 대운과 동일한 순행/역행 규칙
  const isYangStem = ['甲', '丙', '戊', '庚', '壬'].includes(yearStem);
  const isForward = (gender === 'male' && isYangStem) ||
                    (gender === 'female' && !isYangStem);

  const fortunes: MinorFortune[] = [];
  let currentPillar = hourPillar;

  for (let age = 1; age < majorFortuneStartAge; age++) {
    currentPillar = getNextPillar(currentPillar, isForward);
    fortunes.push({
      age,
      stem: currentPillar.stem,
      branch: currentPillar.branch,
      element: STEM_ELEMENTS[currentPillar.stem],
      description: '' // AI로 생성
    });
  }

  return fortunes;
}
```

---

## 4. API 엔드포인트 설계

### 4.1 새 API 라우트

| 엔드포인트 | 메서드 | 설명 |
|-----------|--------|------|
| `/api/saju/daily` | GET | 오늘의 일운 |
| `/api/saju/daily?date=YYYY-MM-DD` | GET | 특정 날짜 일운 |
| `/api/saju/hourly` | GET | 현재 시운 |
| `/api/saju/hourly?date=YYYY-MM-DD&hour=HH` | GET | 특정 시간 시운 |
| `/api/saju/monthly` | GET | 12개월 월운 |
| `/api/saju/fortune-timeline` | GET | 전체 운세 타임라인 |

### 4.2 요청/응답 예시

**GET /api/saju/daily**
```json
{
  "shareId": "abc123",  // 기존 사주 분석 ID
  "date": "2026-01-02"  // optional, 기본값 오늘
}
```

**응답:**
```json
{
  "success": true,
  "data": {
    "date": "2026-01-02",
    "pillar": {
      "stem": "甲",
      "branch": "子",
      "stemKorean": "갑",
      "branchKorean": "자"
    },
    "score": 78,
    "grade": "good",
    "theme": "새로운 시작에 유리한 날",
    "advice": "오늘은 계획을 세우고 첫걸음을 내딛기 좋은 날입니다.",
    "opportunities": ["새 프로젝트 시작", "중요한 미팅"],
    "cautions": ["과도한 음주", "급한 결정"],
    "activities": ["운동", "독서", "계획 수립"]
  }
}
```

---

## 5. UI 컴포넌트 설계

### 5.1 새 컴포넌트

```
components/saju/
├── fortune/
│   ├── DailyFortune.tsx      # 일운 카드
│   ├── HourlyTimeline.tsx    # 시운 타임라인
│   ├── MonthlyCalendar.tsx   # 월운 캘린더
│   ├── FortuneOverview.tsx   # 전체 운세 개요
│   └── FortuneMeter.tsx      # 운세 점수 시각화
```

### 5.2 UI 레이아웃 (강헌 철학 반영)

```
┌─────────────────────────────────────────────────────┐
│  🚗 당신의 사주                                      │
│  "불꽃처럼 뜨거운 열정의 소유자"                       │
│  ├── 일간: 丙火 (태양)                               │
│  └── 주요 특성: #리더십 #창의성 #열정                  │
├─────────────────────────────────────────────────────┤
│  🛤️ 현재 대운 (2020-2030)                           │
│  ┌─────────────────────────────────────┐            │
│  │ 丙午 대운 - "화려한 전성기의 도로"    │            │
│  │ 용신(水)과 충돌 → 균형 필요          │            │
│  └─────────────────────────────────────┘            │
├─────────────────────────────────────────────────────┤
│  📅 2026년 세운                                      │
│  ┌─────────────────────────────────────┐            │
│  │ 乙巳년 - 유연한 적응의 해             │            │
│  │ 점수: 75/100 ████████░░              │            │
│  └─────────────────────────────────────┘            │
├─────────────────────────────────────────────────────┤
│  📆 이번 달 (1월)                                    │
│  ┌─────────────────────────────────────┐            │
│  │ 己丑월 - 안정과 준비의 달             │            │
│  │ 좋은 날: 5일, 12일, 23일             │            │
│  └─────────────────────────────────────┘            │
├─────────────────────────────────────────────────────┤
│  ☀️ 오늘의 운세 (1월 2일)                            │
│  ┌─────────────────────────────────────┐            │
│  │ 甲子일 - 새로운 시작의 날             │            │
│  │ ⭐⭐⭐⭐☆ (78점)                      │            │
│  │                                      │            │
│  │ 💡 오늘의 조언                        │            │
│  │ "계획을 세우고 첫걸음을 내딛기 좋은 날" │            │
│  │                                      │            │
│  │ ✅ 좋은 일: 새 프로젝트, 미팅          │            │
│  │ ⚠️ 주의: 과도한 음주, 급한 결정        │            │
│  └─────────────────────────────────────┘            │
├─────────────────────────────────────────────────────┤
│  ⏰ 시간대별 운세                                    │
│  ┌─────────────────────────────────────┐            │
│  │ 현재 寅時 (03-05시) ████░░░░░░ 40점   │            │
│  │ 다음 卯時 (05-07시) ██████░░░░ 65점   │            │
│  │ ...                                  │            │
│  └─────────────────────────────────────┘            │
└─────────────────────────────────────────────────────┘
```

---

## 6. 구현 일정 (예상)

| Phase | 작업 | 예상 복잡도 |
|-------|------|------------|
| **Phase 1** | 일운 + 월운 상세화 | 중간 |
| **Phase 2** | 시운 + 대운 정밀화 | 높음 |
| **Phase 3** | 소운 + UX 개선 | 낮음 |

---

## 7. 참고 자료

### 7.1 명리학 이론
- [명리학 완벽 정리](./명리학-완벽정리.md)
- 강헌 명리학자 영상 핵심 내용

### 7.2 기존 코드
- `lib/saju/agents/temporal-agent.ts` - 세운/월운 계산 참고
- `lib/saju/calculator.ts` - lunar-javascript 사용법
- `lib/saju/pipeline-steps.ts` - AI 프롬프트 패턴

### 7.3 외부 라이브러리
- `lunar-javascript` - 음력/절기 계산
- `@ai-sdk/google` - Gemini AI 연동

---

*문서 작성: 2026-01-02*
*프로젝트: saju-hancom*
