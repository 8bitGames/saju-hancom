# 사주 결과 페이지 운세 탭 재설계 기획서

## 1. 개요

### 1.1 현재 상태
- 탭 구조: [종합 분석] [AI 상담] (2탭)
- 대운(大運): 종합 분석 탭 내 배치 (잘못된 위치)
- 세운(歲運): 미구현
- 월운(月運): 미구현

### 1.2 목표 상태
- 탭 구조: [종합 분석] [운세] [AI 상담] (3탭)
- 운세 탭에 대운/세운/월운 통합
- 각 운세에 "상세보기" 확장 기능 제공

---

## 2. 운세 체계 설명

### 2.1 운(運)의 위계
```
대운(大運) - 10년 주기
  └── 세운(歲運) - 1년 주기
      └── 월운(月運) - 1개월 주기
          └── 일운(日運) - 1일 주기
              └── 시운(時運) - 2시간 주기
```

### 2.2 각 운세의 특징

| 구분 | 주기 | 설명 | 구현 상태 |
|------|------|------|----------|
| 대운 | 10년 | 인생의 큰 흐름, 10년 단위 변화 | ✅ 구현됨 |
| 세운 | 1년 | 해당 연도의 전반적 운세 | ❌ 미구현 |
| 월운 | 1개월 | 월별 운세 흐름 | ❌ 미구현 |
| 일운 | 1일 | 오늘의 운세 | ✅ 구현됨 |
| 시운 | 2시간 | 시간대별 운세 (12시진) | ✅ 구현됨 |

---

## 3. UI 구조 설계

### 3.1 탭 구조
```
┌─────────────────────────────────────────────────────────┐
│  [종합 분석]    [운세]    [AI 상담]                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  운세 탭 선택 시 표시되는 콘텐츠                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 3.2 운세 탭 내부 구조
```
운세 탭
│
├── 📅 대운/세운 섹션
│   ├── 요약 카드
│   │   ├── 현재 대운: "갑자대운 (43-52세)"
│   │   ├── 현재 세운: "2026년 병오년"
│   │   └── 간단한 키워드/점수
│   │
│   ├── [상세보기] 버튼
│   │
│   └── 확장 영역 (상세보기 클릭 시)
│       ├── 대운 상세
│       │   └── 10년 주기 전체 타임라인
│       └── 세운 상세
│           └── 5년 전망 (2026-2030)
│
├── 📆 월운 섹션
│   ├── 요약 카드
│   │   ├── 현재 월운: "1월 무자월"
│   │   └── 이번 달 운세 포인트
│   │
│   ├── [상세보기] 버튼
│   │
│   └── 확장 영역 (상세보기 클릭 시)
│       └── 12개월 상세 운세
│
└── 🌅 오늘의 운세 섹션
    ├── 일운 (오늘의 운)
    └── 시운 (시간별 운) [프리미엄]
```

### 3.3 UI 컴포넌트 상세

#### 요약 카드 디자인
```
┌─────────────────────────────────────────┐
│ 📅 대운/세운                             │
├─────────────────────────────────────────┤
│                                         │
│  현재 대운: 갑자대운 (43-52세)           │
│  ████████░░░░ 진행률 67%                │
│                                         │
│  2026년 세운: 병오년                     │
│  🔥 화(火) 기운 | ⭐ 좋음                │
│  키워드: #발전 #도약 #변화              │
│                                         │
│           [상세보기 ▼]                   │
└─────────────────────────────────────────┘
```

#### 확장 영역 디자인
```
┌─────────────────────────────────────────┐
│ 대운 상세                               │
├─────────────────────────────────────────┤
│ [타임라인 그래프]                        │
│ 1대운 ─ 2대운 ─ 3대운 ─ 4대운(현재) ─ ...│
│                                         │
│ 현재 대운 분석:                          │
│ - 주제: 성장과 발전의 시기               │
│ - 기회: [...]                           │
│ - 주의: [...]                           │
├─────────────────────────────────────────┤
│ 세운 상세 (5년 전망)                     │
├─────────────────────────────────────────┤
│ 2026년 병오년 ⭐⭐⭐⭐ 좋음             │
│ 2027년 정미년 ⭐⭐⭐ 보통               │
│ 2028년 무신년 ⭐⭐⭐⭐ 좋음             │
│ 2029년 기유년 ⭐⭐ 주의                 │
│ 2030년 경술년 ⭐⭐⭐ 보통               │
│                                         │
│           [상세보기 ▲]                   │
└─────────────────────────────────────────┘
```

---

## 4. API 설계

### 4.1 세운(歲運) API

**엔드포인트:**
```
GET /api/saju/fortune/yearly?shareId={shareId}&years={5}
POST /api/saju/fortune/yearly
Body: { shareId: string, years?: number }
```

**응답 스키마:**
```typescript
interface YearlyFortuneResponse {
  success: boolean;
  data: {
    currentYear: number;
    fortunes: YearlyFortune[];
  };
}

interface YearlyFortune {
  year: number;
  pillar: {
    stem: string;         // 천간 (예: "丙")
    branch: string;       // 지지 (예: "午")
    stemKorean: string;   // "병"
    branchKorean: string; // "오"
    fullName: string;     // "병오"
    element: Element;     // "fire"
    elementKorean: string; // "화"
  };
  analysis: {
    score: number;        // 0-100
    grade: "excellent" | "good" | "average" | "caution";
    keywords: string[];
  };
  natalInteraction: {
    harmonies: string[];  // 합
    clashes: string[];    // 충
    punishments: string[]; // 형
  };
  usefulGodRelation: "support" | "against" | "neutral";
  interpretation?: {      // AI 해석 (프리미엄)
    theme: string;
    description: string;
    opportunities: string[];
    challenges: string[];
  };
}
```

### 4.2 월운(月運) API

**엔드포인트:**
```
GET /api/saju/fortune/monthly?shareId={shareId}&year={2026}
POST /api/saju/fortune/monthly
Body: { shareId: string, year?: number }
```

**응답 스키마:**
```typescript
interface MonthlyFortuneResponse {
  success: boolean;
  data: {
    year: number;
    currentMonth: number;
    fortunes: MonthlyFortune[];
  };
}

interface MonthlyFortune {
  month: number;          // 1-12
  lunarMonth: number;     // 음력 월
  pillar: {
    stem: string;
    branch: string;
    stemKorean: string;
    branchKorean: string;
    fullName: string;     // "무자"
    element: Element;
    elementKorean: string;
  };
  seasonInfo: {
    startDate: string;    // 절입일
    endDate: string;
    solarTerm: string;    // 절기명
  };
  analysis: {
    score: number;
    grade: "excellent" | "good" | "average" | "caution";
    keywords: string[];
  };
  natalInteraction: {
    harmonies: string[];
    clashes: string[];
    punishments: string[];
  };
  yearlyInteraction: {
    description: string;  // 세운과의 관계
  };
  usefulGodRelation: "support" | "against" | "neutral";
  interpretation?: {
    theme: string;
    description: string;
    focusAreas: string[];
    luckyDays: number[];
  };
}
```

---

## 5. 컴포넌트 구조

### 5.1 파일 구조
```
components/saju/
├── FortunePanel.tsx              # 기존 (일운/시운)
├── fortune/                      # 신규 디렉토리
│   ├── FortuneTab.tsx            # 운세 탭 메인 컨테이너
│   ├── MajorYearlySection.tsx    # 대운/세운 통합 섹션
│   ├── YearlyFortuneSection.tsx  # 세운 전용 컴포넌트
│   ├── MonthlyFortuneSection.tsx # 월운 전용 컴포넌트
│   ├── FortuneCard.tsx           # 운세 요약 카드
│   ├── ExpandableSection.tsx     # 상세보기 확장 UI
│   └── index.ts                  # exports
```

### 5.2 컴포넌트 계층
```
FortuneTab
├── MajorYearlySection
│   ├── FortuneCard (대운 요약)
│   ├── FortuneCard (세운 요약)
│   └── ExpandableSection
│       ├── MajorFortuneSection (기존 재활용)
│       └── YearlyFortuneSection
│
├── MonthlyFortuneSection
│   ├── FortuneCard (월운 요약)
│   └── ExpandableSection
│       └── MonthlyFortuneDetail
│
└── DailyHourlySection
    ├── DailyFortuneSection (기존 재활용)
    └── HourlyFortuneSection (기존 재활용)
```

### 5.3 Props 인터페이스

```typescript
// FortuneTab.tsx
interface FortuneTabProps {
  shareId: string;
  birthYear: number;
  isPremium: boolean;
  onUpgradeClick: () => void;
  isLoadingShareId?: boolean;
}

// MajorYearlySection.tsx
interface MajorYearlySectionProps {
  shareId: string;
  birthYear: number;
  isPremium: boolean;
  onUpgradeClick: () => void;
  defaultExpanded?: boolean;
}

// YearlyFortuneSection.tsx
interface YearlyFortuneSectionProps {
  data: YearlyFortune[] | null;
  isLoading: boolean;
  currentYear: number;
  isPremium: boolean;
}

// MonthlyFortuneSection.tsx
interface MonthlyFortuneSectionProps {
  shareId: string;
  year: number;
  isPremium: boolean;
  onUpgradeClick: () => void;
}

// FortuneCard.tsx
interface FortuneCardProps {
  title: string;
  subtitle: string;
  score?: number;
  grade?: string;
  keywords?: string[];
  element?: Element;
  isPremium?: boolean;
  locked?: boolean;
}

// ExpandableSection.tsx
interface ExpandableSectionProps {
  title: string;
  defaultExpanded?: boolean;
  children: React.ReactNode;
  isPremium?: boolean;
  onUpgradeClick?: () => void;
}
```

---

## 6. 데이터 흐름

### 6.1 로딩 시퀀스
```
사용자가 운세 탭 클릭
    │
    ▼
FortuneTab 마운트
    │
    ├── MajorYearlySection
    │   ├── fetch /api/saju/fortune/major
    │   └── fetch /api/saju/fortune/yearly (lazy, 상세보기 시)
    │
    ├── MonthlyFortuneSection
    │   └── fetch /api/saju/fortune/monthly (lazy, 상세보기 시)
    │
    └── DailyHourlySection
        ├── fetch /api/saju/fortune/daily
        └── fetch /api/saju/fortune/hourly (프리미엄)
```

### 6.2 캐싱 전략
- 대운: 변하지 않으므로 장기 캐싱
- 세운: 연 단위 캐싱 (1년)
- 월운: 월 단위 캐싱 (1개월)
- 일운: 일 단위 캐싱 (1일)
- 시운: 2시간 단위 캐싱

---

## 7. 구현 로드맵

### Phase 1: 기반 작업 (백엔드)
**예상 파일:**
- `lib/saju/fortune/yearly.ts` - 세운 계산 로직
- `lib/saju/fortune/monthly.ts` - 월운 계산 로직
- `app/api/saju/fortune/yearly/route.ts` - 세운 API
- `app/api/saju/fortune/monthly/route.ts` - 월운 API

**작업 내용:**
1. 세운 계산 로직 구현
   - 년주 간지 계산
   - 원국과의 상호작용 분석
   - 점수화 및 등급 산출
2. 월운 계산 로직 구현
   - 월주 간지 계산 (절입일 기반)
   - 원국/세운과의 복합 분석
3. API 엔드포인트 구현

### Phase 2: UI 컴포넌트 (프론트엔드)
**예상 파일:**
- `components/saju/fortune/FortuneTab.tsx`
- `components/saju/fortune/MajorYearlySection.tsx`
- `components/saju/fortune/YearlyFortuneSection.tsx`
- `components/saju/fortune/MonthlyFortuneSection.tsx`
- `components/saju/fortune/FortuneCard.tsx`
- `components/saju/fortune/ExpandableSection.tsx`

**작업 내용:**
1. 공통 컴포넌트 구현 (FortuneCard, ExpandableSection)
2. 세운 섹션 구현
3. 월운 섹션 구현
4. 대운/세운 통합 섹션 구현
5. FortuneTab 메인 컨테이너 구현

### Phase 3: 통합
**수정 파일:**
- `app/[locale]/(main)/saju/result/SajuResultContent.tsx`

**작업 내용:**
1. ContentTab 타입 변경: `"analysis" | "fortune" | "chat"`
2. 운세 탭 버튼 추가
3. 운세 탭 콘텐츠 영역에 FortuneTab 연결
4. 분석 탭에서 대운 섹션 제거

### Phase 4: 다국어 지원
**수정 파일:**
- `messages/ko.json`
- `messages/en.json`

**작업 내용:**
1. 운세 탭 관련 한국어 텍스트 추가
2. 영문 번역 추가

### Phase 5: 프리미엄 기능 (선택)
**작업 내용:**
1. 세운 5년 이상 확장 (프리미엄)
2. 월운 상세 AI 해석 (프리미엄)
3. 프리미엄 잠금 UI 적용

---

## 8. 프리미엄 기능 구분

| 기능 | 무료 | 프리미엄 |
|------|------|----------|
| 대운 요약 | ✅ | ✅ |
| 대운 상세 | 3개 대운 | 전체 |
| 세운 요약 | ✅ 현재년도 | ✅ |
| 세운 상세 | ❌ | ✅ 5년 |
| 월운 요약 | ✅ 현재월 | ✅ |
| 월운 상세 | ❌ | ✅ 12개월 |
| 일운 | ✅ | ✅ |
| 시운 | ❌ | ✅ |
| AI 해석 | ❌ | ✅ |

---

## 9. 파일 변경 요약

### 신규 생성
```
lib/saju/fortune/
├── yearly.ts                    # 세운 계산
└── monthly.ts                   # 월운 계산

app/api/saju/fortune/
├── yearly/route.ts              # 세운 API
└── monthly/route.ts             # 월운 API

components/saju/fortune/
├── FortuneTab.tsx               # 운세 탭 메인
├── MajorYearlySection.tsx       # 대운/세운 통합
├── YearlyFortuneSection.tsx     # 세운 전용
├── MonthlyFortuneSection.tsx    # 월운 전용
├── FortuneCard.tsx              # 운세 카드
├── ExpandableSection.tsx        # 확장 섹션
└── index.ts                     # exports
```

### 수정
```
app/[locale]/(main)/saju/result/SajuResultContent.tsx  # 탭 구조 변경
lib/saju/fortune/index.ts                              # export 추가
messages/ko.json                                       # 다국어
messages/en.json                                       # 다국어
```

---

## 10. 기술적 고려사항

### 10.1 기존 코드 재활용
- `MajorFortuneSection`: 대운 상세보기에서 그대로 활용
- `DailyFortuneSection`: 일운 표시에 그대로 활용
- `HourlyFortuneSection`: 시운 표시에 그대로 활용
- `generateMajorFortuneList()`: 대운 계산 로직 재활용
- `HEAVENLY_STEMS`, `EARTHLY_BRANCHES`: 간지 상수 재활용

### 10.2 성능 최적화
- Lazy loading: 상세보기 클릭 시에만 상세 데이터 fetch
- 캐싱: 각 운세별 적절한 캐시 전략 적용
- 병렬 요청: 독립적인 API는 병렬로 호출

### 10.3 접근성
- 키보드 네비게이션 지원
- 스크린 리더 호환
- 적절한 ARIA 레이블

---

*작성일: 2026-01-04*
*버전: 1.0*
